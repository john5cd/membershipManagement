package org.example.model;

/* interface representing the blueprint of all subscriptions. Its usage is mostly for all subscriptions
 * to share the same base class.
 */
public interface Subscription {
}
