package org.example.model;

/* interface representing the blueprint of all activities. Its usage is mostly for all activities
 * to share the same base class.
 */

public interface Activity {
    int MAX_NUMBER_OF_PARTICIPANTS = 6;
}
