package org.example.model;

// Helper "class". An enum representing the possible subscription plans (8 or 15 visits).
public enum SubscribedVisits {
    EIGHT_VISITS_SUBSCRIPTION,
    FIFTEEN_VISITS_SUBSCRIPTION
}
